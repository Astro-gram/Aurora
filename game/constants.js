const stageBounds = {
    w: 1000,
    h: 400
};

const srcMap = [
    {
        src: "test.png",
        id: "backpack"
    },
    {
        src: "test2.png",
        id: "x"
    }
];

const audioMap = {
    test: "audio.wav"
};

const parentFolder = "../game/";

export { stageBounds, srcMap, audioMap, parentFolder };