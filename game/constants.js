const stageBounds = {
    w: 1000,
    h: 400
};

const srcMap = [
    { src: "test.png", id: "backpack" }
]

const audioMap = {
    test: "audio.wav"
}

const parentFolder = "../game/";

export { stageBounds, srcMap, audioMap, parentFolder };